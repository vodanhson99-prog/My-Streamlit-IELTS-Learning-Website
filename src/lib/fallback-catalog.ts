import { PracticeTest } from "./ielts"

// ponytail: fallback tests cleared per user request; tests populate via live scraping and local cache
export const FALLBACK_PRACTICE_TESTS: PracticeTest[] = []
